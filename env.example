# Configuração do MongoDB
# URL de conexão com o banco de dados MongoDB
# Exemplo local: mongodb://localhost:27017/aprovatec
# Exemplo MongoDB Atlas: mongodb+srv://usuario:senha@cluster.mongodb.net/aprovatec
MONGO_URI=mongodb://localhost:27017/aprovatec

# Configuração JWT
# Chave secreta para assinatura e verificação dos tokens JWT
# IMPORTANTE: Use uma string aleatória e segura em produção
# Você pode gerar uma com: openssl rand -base64 32
JWT_SECRET=seu_secret_jwt_aqui_altere_em_producao

# Configuração do Servidor
# Porta em que o servidor irá rodar (opcional, padrão: 3000)
PORT=3000

# Configuração do Redis (Opcional)
# URL de conexão com o Redis para cache
# Se não fornecido, a aplicação funcionará sem cache
# Exemplo local: redis://localhost:6379
# Exemplo Upstash: redis://default:senha@host.upstash.io:6379
# Exemplo Redis Cloud: rediss://default:senha@host.redis.cloud:6379
REDIS_URL=redis://localhost:6379

# Configuração do Frontend (Para CORS)
# URL do frontend que consumirá esta API
# Exemplo local: http://localhost:3000
# Exemplo Vercel: https://seu-projeto.vercel.app
FRONTEND_URL=http://localhost:3000
